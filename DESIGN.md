# 生词主动回忆小程序 — 完整需求文档（供主机智能体实现，龙虾复核）

> 版本：v2.0 需求定稿 · 2026-08-31
> 用途：交给主机智能体照此实现，龙虾负责最终复核一致性。

---

## 一、产品定位

「主动回忆式」学术生词背词器（active recall + retrieval practice）。
核心：给英文单词 → 用户自己打英文释义 → AI 判断对错 → 才叫真理解。
不是「给词+翻译被动背」。

## 二、技术栈（已定，不要改）

- **Windows 本地**，网页版，只在电脑用，不要手机、不要公网
- 后端：**Python + Flask**（用户环境：Anaconda base，已装 Flask 3.1.2 + requests 2.32.5）
- 数据库：**SQLite**（单文件 vocab.db）
- AI：**DeepSeek API**（deepseek-chat 模型）
- 前端：单页 HTML + 原生 JS + CSS，界面轻盈简洁不臃肿
- 运行：`python app.py` → 浏览器开 `http://127.0.0.1:5000`

## 三、数据库结构（SQLite）

### 表 words
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| word | TEXT UNIQUE NOT NULL | 单词 |
| note | TEXT NOT NULL | 备注栏（同义替换 + 英文释义 合并在一栏） |
| zh | TEXT | 中文翻译（可空） |
| example | TEXT | 例句（可空） |
| in_learn | INTEGER DEFAULT 0 | 是否加入背诵清单（0/1） |
| source | TEXT | 'manual'（自录）或 'awl' |
| sublist | INTEGER DEFAULT 0 | AWL 子表号（非 AWL = 0） |
| created_at | TEXT | 创建时间 |

### 表 reviews（学习/复习记录）
| 字段 | 说明 |
|------|------|
| word_id | 外键 |
| review_date | 自然日 YYYY-MM-DD |
| result | 'correct' / 'wrong'（对应理解对/错） |
| last_input | 用户当时的输入 |

### 表 notebook（两个错题本）
| 字段 | 说明 |
|------|------|
| word_id | 外键 |
| type | 'spelling'（拼写本）或 'understand'（理解本） |
| added_at | 加入时间 |

### 表 daily_done（防当天重复）
| 字段 | 说明 |
|------|------|
| word_id | 外键 |
| done_date | 自然日 YYYY-MM-DD |

## 四、核心概念区分（关键！）

**「词库」≠「背诵清单」**

- **词库**：所有词（AWL 570 + 用户自录），用于浏览、挑选
- **背诵清单**：用户手动勾选（in_learn=1 的词），**只有这些词才进入背词流程**
- AWL 预置进词库时，**in_learn 默认 = 0**（不自动进背诵），由用户手动挑选

## 五、AWL 学术词库

- 预置 **AWL 570 词**：sublist 1-9 各 60 词 + sublist 10 共 30 词 = 570
- 数据：headword + sublist + related word forms（相关词形）
- 预置方式：启动时若词库为空，自动 seed；note 字段初值用 related word forms
- 导师给的词表来源：https://www.eapfoundation.com/vocab/academic/awllists/

## 六、功能清单

### 1. 背词（核心，第一个 tab）
- **只从背诵清单（in_learn=1）取词**
- 一页 20-26 词（默认 20），左词右填英文释义
- 每日学习量可手动设置（例：200 词设 100 → 拆 5 页；设 10 → 只 1 页）
- 每天**随机打乱**；**当天学过的词当天不重复出现**
- 每填完一页点「提交并下一页」→ 该页 AI 后台判定

### 2. 录词（含自动生成）
- 字段：单词 + 备注（同义/英文释义，必填）+ 中文翻译（可空）+ 例句（可空）
- 是否需要加入背诵清单：勾选（默认勾选）
- **✨自动生成释义按钮**：只输单词 → 调 DeepSeek 自动填「note + zh + example」→ 用户可改再保存
- 自动生成接口 `/api/wordgen`，DeepSeek 返回 JSON：{note, zh, example}

### 3. 词库（浏览 + 挑词）
- 三筛：AWL 词库 / 自录词 / 全部
- 可选「只看已加入背诵」
- 每个词旁有 ➕/✅ 开关，点击切换「加入/移出背诵清单」
- 显示 sublist 号（AWL 词）或「自录」标记

### 4. AI 判定（四档 + 两个笔记本）

AI 对用户写的英文释义做两步判断：① 是否理解 ② 拼写/表达问题。

| 结果 | 去向 |
|------|------|
| ✅ 理解正确 | 通过（不进任何本） |
| ⚠️ 拼写错误（不影响理解） | **不自动进**，AI 下划线标出，用户点「+」手动加 |
| ❌ 理解错误 | **自动进「理解笔记本」** |

- AI 返回 include：understand(bool)、misspell(词数组)、advice(中文建议)
- **拼写本不自动进**：把用户输入原样贴出、下划线标出 misspell 里的词，每个词旁有「+」，用户手动决定加不加
- 若用户「手误加了明明会但打错」的熟悉词，笔记本里有删除按钮

### 5. 两个笔记本
- **拼写笔记本**（type=spelling）：手动加
- **理解笔记本**（type=understand）：理解错误自动进
- **共同规则**：
  - 连续 **3 个自然日**答对 → 自动移出（自然日，不是次数）
  - 有「删除」按钮，手动移除

### 6. 标准答案三层展示
1. 默认只显示英文释义（note）
2. 「显示中文」按钮 → 展开全面中文翻译（zh）
3. 「例句」按钮 → 展开例句（example）

### 7. 课件上传 + 例句自动提取（待实现，阶段③）
- 上传课件（txt 优先；pdf/word 后续）
- 自动在课件文本中搜索包含某单词的句子，作为例句

### 8. 导出
- 「关键词 | 备注」两列，一行一个词条
- 格式参照语言班关键词表（左 keyword 右 notes）

### 9. 设置
- DeepSeek API key（填写存储）
- 每日学习量（daily_limit）
- 每页词数（page_size）

## 七、关键交互细节（用户特别要求，勿偏离）

1. **一页多词，不是一页一词**——用户说「一页不止一个单词，会打断学习状态」
2. **批量判定**：整页填完后一次提交，AI 批量判定；结果在每个词下方就地展开
3. **「显示中文」要点击才展开**——不要默认显示中文，用户要「先自己回忆，实在不懂再点开对照」
4. **界面轻盈**——不要臃肿、不要花哨
5. **拼写错误加限制条件**——「允许不影响理解的拼写错误」；「如果错得太离谱再加入笔记本」
6. **错词由用户最终拍板**——AI 只提示，加不加进拼写本由用户点「+」决定

## 八、DeepSeek 调用约定

- 模型：`deepseek-chat`
- 判词 prompt 要求返回 JSON：{understand, misspell[], advice}
- 生成释义 prompt 要求返回 JSON：{note, zh, example}
- 用 `response_format: {"type": "json_object"}`

## 九、待办（后续阶段）

- 阶段③：课件 PDF/Word 解析（扫描件需 OCR）
- 阶段④：导出完善、每日学习量设置细化

## 十、已确认但尚未实现/待复核的点

- [ ] API key 已暴露在聊天记录，建议安重新生成 DeepSeek key
- [ ] 课件自动抓例句：文字型 PDF/txt 可抓，扫描件需 OCR（阶段③处理）
- [ ] 是否需要在「拼写笔记本」区分「手误」vs「真不会」（用户已说用删除按钮手动处理，暂不额外区分）
